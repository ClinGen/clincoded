{
    "title": "Assessment",
    "id": "/profiles/assessment.json",
    "description": "Schema for storing and using assessments",
    "$schema": "http://json-schema.org/draft-04/schema#",
    "type": "object",
    "required": ["submitted_by", "value", "date_created"],
    "identifyingProperties": ["uuid"],
    "additionalProperties": false,
    "mixinProperties": [
        { "$ref": "mixins.json#/schema_version" },
        { "$ref": "mixins.json#/uuid" },
        { "$ref": "mixins.json#/submitted" }
    ],
    "properties": {
        "schema_version": {
            "default": "1"
        },
        "value": {
            "title": "Value",
            "description": "Value of assessment",
            "type": "string",
            "enum": [
                "Supports",
                "Review",
                "Contradicts"
            ]
        },
        "active": {
            "title": "Active",
            "description": "Boolean switch for activation of the assessment",
            "type": "boolean",
            "default": true
        }
    },
    "columns": {
        "uuid": {
            "title": "Assessment",
            "type": "string"
        },
        "value": {
            "Title": "Value",
            "type": "string"
        }
    }
}
